<% 
	the_label = "#{control_action_label} #{label_name('for')} #{item_name_link(@enchufado)}"
	@item = @subplug
%>


<%= content_tag :h6, "#{the_label}".html_safe, :class => "subheader" %>
<hr/>
<br/>

<%= render 'shared/new_edit_multipart_zurb', :item => @item %>
