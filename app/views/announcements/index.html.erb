<div class="block">

	<%#= render :partial => '/groups/secondary_navigation' %>  

	<div class="content">   

		<h2 class="title"><%= get_collection_label(@announcements) %></h2>

		<div class="inner">

			<% if object_counter(@announcements) > 0 %>

			<table class="table">

			<%= render :partial => 'classified', :collection => @announcements %>
				
			</table>

			<% end %>
			<%= render :partial => 'shared/action_bar', :locals => { :collection => @announcement } %>

		</div>
	</div>
</div>

<% content_for :sidebar, render(:partial => 'sidebar') -%>