<%
is_not_new_item = !is_action('new')
%>

<%= item_new(item) %>

<% if is_not_new_item %>
	<dd class="<%= get_first_active %>"><%= link_to(label_name(:announcements) , announcements_path) %></dd>
	<dd class="<%= get_active('announcement_list') %>"><%= link_to(label_name(:announcements_previous) , list_announcements_path) %></dd>
<% end -%>
