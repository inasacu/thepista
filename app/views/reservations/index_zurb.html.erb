<%
	the_label = label_name(:reservations)
	content_for(:title, the_label)
	
	the_new_label = "#{the_label} (#{item_name_link(@installation)})"
	left_label = ""
	right_label = "#{item_name_link(@installation)}<br/>#{nice_day_time_wo_year_exact(Time.zone.now)}".html_safe
%>


<%= content_tag :h6, right_label, :class => "subheader" %>
<hr />

<%= render 'shared/calendar_zurb' %>



<%#= render 'reservations/full_calendar_zurb', :first_day => @first_day, :time_frame => @time_frame, :installation => @installation %>	
<%#= render 'reservations/new_reservation_zurb', :first_day => @first_day, :time_frame => @time_frame, :installation => @installation %>


