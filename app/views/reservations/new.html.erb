<div class="block">

	<%= get_secondary_navigation(@venue) %>

	<div class="content">  

		<h2 class="title">
			<%= "#{control_action_label} #{label_name('for')} " %>
			<%= "#{item_name_link(@installation)}, #{item_name_link(@venue)}" %>
		</h2>

		<div class="inner">

			<% form_for :reservation, :url => reservations_path, :html => { :class => :form } do |f| -%>
			<%= render :partial => "form", :locals => {:f => f, :label_name => label_name(:create)} %>
			<% end -%>

		</div>

	</div>

</div>

