<% content_for :sidebar, render(:partial => 'venues/sidebar_show') -%>
<% content_for :sidebar, render(:partial => 'shared/sidebar_show_reservations', :locals =>{:installations => @installations}) %>

<% content_for :sidebar, render(:partial => '/shared/sidebar_basic_legend', 
					:locals => {:the_render => '/shared/sidebar_show_venue', 
					:the_label => label_name('manager_option')}) if current_user.is_manager_of?(@venue) %>

<% content_for :sidebar, render(:partial => 'shared/share_object', :locals => {:venue => @venue}) if @venue -%>
