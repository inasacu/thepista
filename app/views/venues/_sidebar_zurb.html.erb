
<%#= render 'sidebars/sidebar_create_zurb', :item => @venue %>
<%#
	the_label= label_name(:options)
%>


<div class="row">



<div class="twelve columns">
	<p><%#= option_image_link('message') %>&nbsp;&nbsp;<%#= link_to(label_name(:send_message_to), new_message_path(:id => @venue.all_the_managers.first)) %></p>
</div>

</div>

<%# content_for :sidebar, render(:partial => 'venues/sidebar_show') -%>
<%# content_for :sidebar, render(:partial => 'sidebars/sidebar_show_reservations', :locals =>{:installations => @installations}) %>

<%# content_for :sidebar, render(:partial => '/sidebars/sidebar_basic_legend', 
					:locals => {:the_render => '/sidebars/sidebar_show_venue', 
					:the_label => label_name('manager_option')}) if is_current_manager_of(@venue) %>

<%# content_for :sidebar, render(:partial => 'shared/share_object', :locals => {:venue => @venue}) if @venue -%>
