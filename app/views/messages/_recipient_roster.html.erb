<% display = false %>

<% if current_user.is_member_of?(@schedule.group) or current_user.is_member_of?(@schedule.group_visit) %>

<% unless @schedule.convocados.empty? %>

<%= render :partial => '/messages/recipient_roster_list', 
:locals => {:label => t(:roster), :schedule => @schedule, :recipients => @schedule.the_roster, :checked => checked } %>
<% end %>

<% unless @schedule.last_minute.empty?  %>

<%= render :partial => '/messages/recipient_roster_list', 
:locals => {:label => t(:last_minute), :schedule => @schedule, :recipients => @schedule.the_last_minute, :checked => checked } %>
<% end %>

<% if display %>
<% unless @schedule.no_shows.empty?  %>

<%= render :partial => '/messages/recipient_roster_list', 
:locals => {:label => t(:no_shows), :schedule => @schedule, :recipients => @schedule.the_no_show, :checked => checked } %>
<% end %> 

<% unless @schedule.unavailable.empty?  %>

<%= render :partial => '/messages/recipient_roster_list',  
:locals => {:label => t(:unavailable), :schedule => @schedule, :recipients => @schedule.the_unavailable, :checked => checked } %>
<% end %>
<% end %>

<% end %>