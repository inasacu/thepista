
  <div class="group">
    <%= f.label :subject, "Subject", :class => :label %>
    <%= f.text_field :subject, :class => 'text_field' %>
    <span class="description"><%= label_name('name', 'description') %></span>
  </div>

  <div class="group">
    <%= f.label :body, "Body", :class => :label %>
    <%= f.text_area :body, :class => 'text_area' %>
    <span class="description"><%= label_name('name', 'description') %></span>
  </div>

<div class="clear"></div> 

<div class="group navform">
	<%= submit_tag @message.new_record? ? label_name(:create) : label_name(:edit), 
			:class => "button" %> <%= label_name(:or) %> <%= link_to label_name(:cancel), messages_path %>
</div>