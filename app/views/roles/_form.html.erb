
  <div class="group">
    <%= f.label :concept, label_name('concept'), :class => :label %>
    <%= f.text_field :name, :class => 'textfield' %>
    <span class="description"><%= control_label('name_description') %></span>
  </div>

  <div class="group">
    <%= f.label :concept, label_name('concept'), :class => :label %>
    <%= f.text_field :authorizable_type, :class => 'textfield' %>
    <span class="description"><%= control_label('name_description') %></span>
  </div>

  <div class="group">
    <%= f.label :concept, label_name('concept'), :class => :label %>
    <%= f.text_field :authorizable_id, :class => 'textfield' %>
    <span class="description"><%= control_label('name_description') %></span>
  </div>

<div class="clear"></div> 

<div class="group navform">
	<%= submit_tag @role.new_record? ? control_label('create') : control_label('edit'), 
			:class => "button" %> <%= label_name(:or) %> <%= link_to label_name(:cancel), roles_path %>
</div>