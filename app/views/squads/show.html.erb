<% 
	is_user_manager = (current_user.is_manager_of?(@squad))
%>

<div class="block">

  <%= render :partial => '/cups/secondary_navigation' %>   
  
  <div class="content">
	        
  <h2 class="title"><%= @squad.name %></h2>

    <div class="inner">

	<!-- new table -->
	<table>
	<thead>

	<tr>
	<th colspan="2">
		<div class="description">
			<span class="q">
				<%=sanitize @squad.description.blank? ? control_label('description_no') : @squad.description %>
			</span>
		</div>
	</th>

	</tr>
	</thead>

	<tr>
	<td valign="top" width="105"><%= squad_show_photo(@squad, current_user) %></td>

	<td>
	<table border="1">
		
		<%= tr_td_action(:name, @squad.name) %>
		<%#= tr_td_action(:sports, @squad.sport.name) %>
		<%#= tr_td_action(:time_zone, @squad.time_zone) %>
		<%#= tr_td_action(:starts_at, nice_day_of_week(@squad.starts_at)) %>
		<%#= tr_td_action(:ends_at, nice_day_of_week(@squad.ends_at)) %>
		<%#= tr_td_action(:deadline_at, nice_day_of_week(@squad.deadline_at)) %>
		<%#= tr_td_action(:squads, t(:has_cup_squads, :count => @squad.squads.count)) %>
		<%#= tr_td_action(:games, t(:has_cup_games, :count => @squad.games.count)) %>	
		
		<tr>
			<%#= content_tag :td, link_to(label_name(:standing), standing_path(:id => @squad)), :class => "label" %>
		</tr>

	   <tr>
	     <%#= content_tag :td, cup_list(@squad.squads), :class => get_the_action %>
	   </tr>
	   <tr>
		<%#= render :partial => '/shared/show_images', :locals => {:collection => @squad.squads} %>			
	   </tr>	

		</table>

	</td>
	</tr>

	</table>
	<!-- new table -->


			 
    </div>
  </div>
</div>

<% content_for :sidebar, render(:partial => 'sidebar_show') -%>