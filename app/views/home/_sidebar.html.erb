<div class="block">
	<h3><%= control_label('information') %></h3>
	<div class="content">
		
		<% if current_user.has_pending_petition?(current_user) %>
		<p><%= link_to(label_name(:petitions), petition_path)  %></p>
		<% end %>
		
	</div>
</div>

<%= render :partial => '/shared/active_comment' if current_user %>