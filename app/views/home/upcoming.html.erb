<%  
	the_label = label_name(:upcoming)
	content_for(:title, the_label)
%> 


<%= render 'home/upcoming_schedule_zurb', :my_schedules => @upcoming_schedules, :the_label => the_label unless @upcoming_schedules.empty? %>

<%= render 'home/upcoming_cup_zurb', :upcoming_cups => @upcoming_cups, :the_label => label_name(:upcoming)  unless @upcoming_cups.empty? %>
	
<%= render 'home/upcoming_game_zurb', :my_schedules => @upcoming_games, :the_label => the_label unless @upcoming_games.empty? %>

<% unless @upcoming %>

<div class="row">
	<div class="twelve columns">   

		<%= show_heading(label_name(:upcoming_no)) %>

	</div> 
</div>

<% end -%>
