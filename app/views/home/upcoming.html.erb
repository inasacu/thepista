<%  
	# has_schedule ||= false	
	# has_schedule = true unless upcoming_schedules.empty? 
	# 
	# has_game ||= false	
	# has_game = true unless upcoming_games.empty?
	# 
	# has_cup ||= false	
	# has_cup = true unless upcoming_cups.empty?
	
	the_label = label_name(:upcoming)
	content_for(:title, the_label)
%> 

<div class="block">

  <%= get_secondary_navigation %>
  
  <div class="content">   
  
 	<%= show_heading(the_label) %>

    <div class="inner">

	<%= render :partial => "home/all_upcoming_schedules", :locals => {:upcoming_schedules => @upcoming_schedules} %>

    </div>

    <div class="inner">

	<%= render :partial => "home/upcoming_cup", :locals => {:upcoming_cups => @upcoming_cups} %>

    </div>

    <div class="inner">

	<%= render :partial => "home/upcoming_game", :locals => {:upcoming_games => @upcoming_games} %>
	
    </div>

  </div> 
</div>

<% content_for :sidebar, render(:partial => 'home/sidebar_home') -%>






