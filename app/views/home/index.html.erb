<% 
if current_user
	
	the_label = label_name(:latest_activities)
	content_for(:title, the_label)
%> 
	<div class="block">
		<%= render :partial => 'secondary_navigation' -%>
		<div class="content">   
			<h2 class="title"><%= label_name(:latest_activities) %></h2>
			<div class="inner">	
					
			<table class="table" id="<%= get_the_controller.gsub(' ','_') %>">

				<!-- <tr>             
					<th class="first">&nbsp;</th>
					<th>&nbsp;<%= label_name(:latest_activities) %></th>
					<th>&nbsp;</th>
					<th>&nbsp;</th>
					<th class="last">&nbsp;</th>
				</tr> -->
				
				<% @items. each do |item| %>				
				<%= render :partial => 'upcoming_home', :locals => {:teammate => item} unless item.class.to_s == 'Teammate'%>				
				<%= render :partial => '/users/petition_box', :locals => {:teammate => item} if item.class.to_s == 'Teammate'%>
				<% end %>
				
			</table>
			
			</div>
		</div>
	</div>
	<% content_for :sidebar, render(:partial => '/shared/sidebar_index') -%>
<% 
else 
%>
	<%= render :partial => 'details' %>	
<% 
end 
-%>