

<%= render 'shared/show_description_zurb', :item => @marker %>
	
	
<div class="row">

	<div class="twelve columns"> 
		
		<table>
		<tbody>
		<tr>
		<td>
			
		<%= render_show_detail_zurb(:name, item_name_link(@marker)) %>
		<%= render_show_detail_zurb(:latitude, @marker.latitude) %>
		<%= render_show_detail_zurb(:longitude, @marker.longitude) %>
		<%= render_show_detail_zurb(:url, @marker.url) %>
		<%= render_show_detail_zurb(:city, @marker.city) %>
		<%= render_show_detail_zurb(:city, @marker.city) %>
		<%= render_show_detail_zurb(:zip, @marker.zip) %>

		
		</td>	
		</tr>
		</tbody>
		</table>
		
	</div>
</div>
        