<% 
@archive ||= false 
%>

<div class="block">

	<%= render :partial => 'secondary_navigation' %>  

	<div class="content">  

		<% if object_counter(@groups) > 0 %>
			<%= render :partial => 'index_full' %>
		<% else %>
			<%= render :partial => 'groups/index_blank' %>
		<% end %>

	</div>
</div>
<% content_for :sidebar, render(:partial => 'sidebar')  if object_counter(@groups) > 0  -%>
