<% 
	the_label = label_name(:scorecards)
	content_for(:title, the_label)
%>
<div class="row">
	 
	<div class="twelve column">  
			<% if object_counter(@groups) > 0 %>
				<%= render :partial => '/groups/index_full_zurb' %>
			<% else %>
				<%= render :partial => '/groups/index_blank_zurb' %>
			<% end %>
	</div>	
</div>

<% content_for :sidebar, render(:partial => '/groups/sidebar_zurb') -%>