<% 	get_first_active ||=""
	get_first_active = get_active("#{get_the_controller}_#{get_the_action}") if is_controller('scorecard') and is_action('index')
%>
<div class="secondary-navigation">

<ul>
<%
	unless is_action('show') 
%>
	<li class="<%= get_first_active %>"><%=  link_to(label_name(:scorecards), scorecards_path) %></li>
	<!-- <li class="<%#= get_active('scorecard_list') %>"><%#=  link_to(label_name(:all_scorecard) , list_scorecards_path) %></li> -->
<%
	else
%>
	<li class="<%= get_first_active('scorecard_show') %>"><%= link_to label_name(:scorecard), scorecard_path(:id => @group) %></li>	
<%
	end
%>
</ul>

<div class="clear"></div>

</div>