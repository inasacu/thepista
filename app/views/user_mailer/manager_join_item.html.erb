<% 	
	the_user = "#{I18n.t(:request_petition)} #{I18n.t(:from)} 
				#{link_to(@user.name, {:host => 'haypista.com', :controller => 'users', :action => 'show', :id => @user}, 
					:title => @user.name)} #{I18n.t(:to_join_email)}".html_safe
	the_name = @item.name	
	the_item = "#{link_to(the_name, {:host => 'haypista.com', :controller => "#{@item.class.to_s}s".downcase, :action => 'show', :id => @item}, :title => the_name)}".html_safe

	the_label = I18n.t(:request_petition) 
%>




<%= "#{the_user} #{the_item}".html_safe %>
<br>
<br>

<% unless @automatic_petition %>

<%= "#{link_to(I18n.t(:accept_player), {:host => 'haypista.com', :controller => 'teammates', 
					:action => 'join_item_accept', :id => @teammate})}".html_safe %>  
<%= "#{link_to(I18n.t(:deny_player), {:host => 'haypista.com', :controller => 'teammates', 
					:action => 'join_item_decline', :id => @teammate})}".html_safe %>
<br>
<br>

<% end %>

