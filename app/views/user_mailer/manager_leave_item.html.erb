<%= "#{I18n.t(:petition_to_join_declined)} #{link_to(@item.name, {:host => 'haypista.com', :controller => "#{item.class.to_s}s".downcase, :action => 'show', :id => @item}, :title => @item.name)}".html_safe %>
<br>
<br>

