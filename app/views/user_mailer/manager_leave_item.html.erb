<%
	the_label = I18n.t(:request_petition) 
%>

<%= render 'user_mailer/header' %>

<%= render  'user_mailer/title', :the_label => the_label %>


<%= "#{I18n.t(:petition_to_join_declined)} #{link_to(@item.name, {:host => 'haypista.com', :controller => "#{item.class.to_s}s".downcase, :action => 'show', :id => @item}, :title => @item.name)}" %>


<%= render 'user_mailer/footer' %>