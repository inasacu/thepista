<% 
	@comment = Comment.new 
	textfield ||= false
%>
<div class="inner">

<% form_for :comment, :url => comments_path, :html => { :class => :form } do |f| -%>
	<h5 class="title"><%= label %></h5>
	<br />
<% 
	if textfield 
%>
		<%= f.text_area :body, :class => 'textfield' -%>&nbsp;&nbsp;<%= f.submit label_name(:share) %>		
<% 
	else 
%>
		<%= f.text_area :body, :class => 'textfield' -%>
		<br /><br />
		<%= f.submit label_name(:share) %>
<% 
	end 
%>
	<%= hidden_field :blog, :id %>
	<%= hidden_field :forum, :id %>
<% end -%>

	<div class="clear"></div>
</div>
