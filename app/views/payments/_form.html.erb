
  <div class="group">
    <%= f.label :concept, label_name('concept'), :class => :label %>
    <%= f.text_field :concept, :class => 'text_field' %>
    <span class="description"><%= label_name('concept', 'description') %></span>
  </div>

  <div class="group">
    <%= f.label :debit_amount, label_name('debit_amount'), :class => :label %>
    <%= f.text_field :debit_amount, :class => 'text_field' %>
    <span class="description"><%= label_name('debit_amount', 'description') %></span>
  </div>

  <div class="group">
    <%= f.label :credit_amount, label_name('credit_amount'), :class => :label %>
    <%= f.text_field :credit_amount, :class => 'text_field' %>
    <span class="description"><%= label_name('credit_amount', 'description') %></span>
  </div>

  <div class="group">
    <%= f.label :description, label_name('description'), :class => :label %>
    <%= f.text_area :description, :class => 'text_area' %>
    <span class="description"><%= label_name('description') %></span>
  </div>

<div class="clear"></div> 

<div class="group navform">
	<%= submit_tag @payments.new_record? ? label_name(:create) : label_name(:edit), 
			:class => "button" %> <%= label_name(:or) %> <%= link_to label_name(:cancel), payments_path %>
</div>