<% 
	the_label = "#{control_action_label} #{label_name(:from)} #{debit_link} #{label_name(:to)} #{item_link}" 
	@item = @payment
	debit_link, debit_object = fee_debit(@fee)
	item_link, item_object = fee_item(@fee)
%>

<%= render 'shared/new_edit_multipart_zurb', :item => @item, :the_label => the_label %>