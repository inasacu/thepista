<div class="block">

	<%= render :partial => 'shared/secondary_navigation' %>

	<%#= render :partial => 'shared/content', :locals => {:content_title => :edit_user} %>  
	<% content_for :content, render(:partial => 'shared/content', :locals => {:content_title => :edit_user}) -%>

	<div class="content">   

		<h2 class="title"><%= control_action_label %></h2>

		<div class="inner">

			<% form_for(@user, :html => { :multipart => true ,  :class => :form }) do |f| %>
			<%# form_for @user, :url => user_path(@user.id), :html => { :class => :form } do |f| -%>

			<%= render :partial => "form", :locals => {:f => f} %>

			<% end -%>

		</div>

	</div>

</div>

<% content_for :sidebar, render(:partial => 'sidebar') -%>