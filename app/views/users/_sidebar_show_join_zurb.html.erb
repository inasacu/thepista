<%
current_user.groups.each do |group| 
	the_group = group.name
	unless @user.is_member_of?(group)  
		if is_current_manager_of(group)
%>
			<p><%= set_image_and_link(join_item_link_to(@user, group, true), 'user_add') %></p> 	
<% 
		end
	else 
		if is_current_manager_of(group)
%> 
			<p><%= set_image_and_link(leave_item_link_to(@user, group), 'group_remove') %></p> 	
<% 
		end
	end 
end 
%>
