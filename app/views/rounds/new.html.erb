<div class="block">

	<%= render :partial => '/tournaments/secondary_navigation' %>

	<div class="content">  

		<h2 class="title"><%= control_action_label %></h2>

		<div class="inner">
	
	  <% form_for(@round, :html => { :multipart => true, :class => :form  }) do |f| %>
		<%= f.hidden_field :tournament_id if @tournament %>
        <%= render :partial => "form", :locals => {:f => f} %>
      <% end -%>

		</div>

	</div>

</div>

<% content_for :sidebar, render(:partial => '/tournaments/sidebar') -%>