<div class="block">

	<%= render :partial => '/tournaments/secondary_navigation' %>  

	<div class="content">  

			<% if object_counter(@rounds) > 0 %>
				<%= render :partial => 'index_full' %>
			<% else %>
				<%= render :partial => 'index_blank' %>
			<% end %>

	</div>
	
</div>

<% content_for :search, render(:partial => 'shared/search') -%>

<% content_for :sidebar, render(:partial => '/tournaments/sidebar') -%>

<% content_for :sidebar, render(:partial => '/tournaments/sidebar_show') -%>
<%# content_for :sidebar, render(:partial => '/fees/sidebar_show_tour') if current_user.is_manager_of?(@tournament) %>