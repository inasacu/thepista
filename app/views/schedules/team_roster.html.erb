<% 
if current_user.is_member_of?(@schedule.group) 
	unless @schedule.convocados.empty? or @schedule.played?
%>
		<%= render :partial => '/schedules/team_roster_box', 
					:locals => {:previous_show => @previous, :next_show => @next, :roster => t(:rosters), 
								:schedule => @schedule, :the_roster => @schedule.the_roster, 
								:match_type => @match_type, :display_type => true} %>
<% 
	end 
	unless @schedule.last_minute.empty? or @schedule.played? 
%>
		<%= render :partial => '/schedules/team_roster_box', 
				   :locals => {:previous_show => @previous, :next_show => @next, :roster => t(:last_minute), :schedule => @schedule, 
							   :the_roster => @schedule.the_last_minute, 
						       :match_type => @match_type, :display_type => true} %>
<% 
	end 
end 
%>