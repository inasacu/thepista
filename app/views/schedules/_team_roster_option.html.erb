<% image = 'status_online.png' if type.id == 1 %>
<% image = 'status_away.png' if type.id == 2 %>
<% image = 'status_busy.png' if type.id == 3 %>
<% image = 'status_offline.png' if type.id == 4 %>

<% 
	the_label = "#{label_name(:change_roster_status) } #{label_name(type.name)}"
	the_confirmation = "#{the_label}?"
%>

<%= link_to image_tag(image, :title => the_label, :size => '22x22'), match_status_path(:id => match.id, :type => type.id), :title => the_label %>
&nbsp;&nbsp;

