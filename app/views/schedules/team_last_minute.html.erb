<% 
	a_roster = !(@schedule.last_minute.empty?)
	the_label = control_action_label("#{a_roster}")
%>

<div class="block">

	<%= render :partial => '/shared/previous_next_message' %>   

		<h2 class="title"><%= the_label %></h2>

		<div class="inner">
<% 
	if a_roster
%>
		<%= render :partial => '/schedules/team_roster_box', 
					:locals => {:previous_show => @previous, :next_show => @next, :roster => label_name(:last_minute), 
								:schedule => @schedule, :the_roster => @schedule.the_last_minute, 
								:match_type => @match_type} %>
<% 
	end 
%>
		</div>
	</div>
</div>

	<% content_for :sidebar, render(:partial => '/schedules/sidebar_show') if @schedule -%>


















