<% 
	the_label = "#{control_action_label} #{label_name('for')} #{item_name_link(@schedule.group)}"
	@item = @schedule
%>



<%= content_tag :h6, "#{the_label}".html_safe, :class => "subheader" %>
<hr/>
<br/>

<%= render 'shared/new_edit_multipart_zurb', :item => @item, :the_label => the_label %>

