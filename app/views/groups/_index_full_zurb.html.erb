<%
	the_label = get_collection_label(@groups)
%>

<div class="row">
	
	<div class="twelve columns">
		
		<div class="foundation-header">
			
			<%= content_tag :h5, the_label %>
			
			<hr />
			
			<%#= content_tag :h6, the_total_label, :class => "subheader" %>
			
		</div>
	</div>
	
</div>


<div class="row">
	<div class="twelve columns"> 
		
		<table>
		  	<thead>
			<tr>             
				<%= content_tag :th, "&nbsp;" %>
				<%= content_tag :th, "#{label_name(:name)}<br />#{label_name(:sport)}" %>
				<%= content_tag :th, "#{label_name(:manager)}" %>
				<%= content_tag :th, "&nbsp;" %>
				<%= content_tag :th, "#{label_name(:scorecard)}" %>
				<%= content_tag :th, "&nbsp;" %>
			</tr>
			</thead>
			
		<tbody>
			
			<% @groups.each do |group| -%>		
				<%= render 'groups/group_full_zurb', :group => group, :display_icons => true %> 
		  	<% end -%>
		
		
			<%# @users.each do |user| 
				schedule = Schedule.last_schedule_played(user)
				the_date = schedule.nil? ? '' : nice_full_date(schedule.starts_at)
		    -%>
		
			<tr>
				<%#= content_tag :td, image_link_small(user) %>					
				<%#= content_tag :td, "#{item_name_link(user)} <br/> #{content_tag(:span, the_date, :class =>'date')}", :class => 'name_and_date' %>
				<%#= content_tag :td, (user.has_group? ? group_list(user.groups) : label_name('groups_none')) %>		 
				<%#= content_tag :td, sanitize(user.city.name) %>
				<%#= content_tag :td, link_to(label_name(:send_message_to), new_message_path(:id => user)) %>	  
			</tr>
			
			<%# end -%>
		
		</tbody>
		</table>
		
	</div>
</div>

<hr/>

<%= render 'shared/action_bar_zurb', :collection => @groups %>
	  



	 