<% a_roster = !(@meet.last_minute.empty?) %>

<div class="block">

	<%= render :partial => 'secondary_navigation' %>  

	<div class="content">   

		<h2 class="title">
			<%= a_roster ? label_name(:last_minute) : label_name("#{get_the_controller}_#{label_name(:last_minute)}") %>
		</h2>

		<div class="inner">
<% 
	if a_roster
%>
		<%= render :partial => '/meets/tour_roster_box', 
					:locals => {:previous_show => @previous, :next_show => @next, :roster => label_name(:last_minute), 
								:meet => @meet, :the_roster => @meet.the_last_minute, 
								:match_type => @match_type} %>
<% 
	end 
%>
		</div>
	</div>
</div>

	<% content_for :sidebar, render(:partial => '/meets/sidebar_show') if @meet -%>


















