
<%= the_form.hidden_field :cup_id %>


<%= set_form_label(the_form, :name) %>
<%= set_form_text_field(the_form, :name, 'large input-text', true) %><br/>

<% if DISPLAY_FREMIUM_SERVICES %>
	<%= set_form_label(the_form, :automatic_petition) %>
	<%= the_form.check_box :automatic_petition, :class => 'textfield' %><br/>
<% else %>
	<%= the_form.hidden_field :automatic_petition %>
<% end %>

<%#= set_form_label(the_form, :description) %>
<%#= set_form_text_area(the_form, :description, true) %>


<%#= set_form_label(the_form, :conditions) %>
<%#= set_form_text_area(the_form, :conditions, true) %>

<% if the_maximo %>

	<%= set_form_label(the_form, :archive) %>
	<%= the_form.check_box :archive, :class => 'textfield' %>
	
<% end %>

