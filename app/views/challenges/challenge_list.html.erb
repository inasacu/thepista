<div class="block">

  <%= render :partial => 'secondary_navigation' %>  

  <div class="content">   
  
    <h2 class="title"><%= I18n.t(:has_challenge_users, :count => @total) %></h2>
	
    <div class="inner">
	
      <table class="table">
	  
		<tr>             
			<th class="first">&nbsp;</th>
			<th><%=label_name(:name) %></th>
			<th><%=label_name(:challenge) %></th>
			<th><%=label_name(:time_zone) %></th>
			<th>&nbsp;<%#=label_name(:last_game_played) %></th>
			<th class="last">&nbsp;</th>
		</tr>
		
        <% @users.each do |user| 
			# cast = Cast.last_cast_played(user)
	    -%>
			<tr>
			  <td><%= image_link_small user %></td>					
			  <td><%= user_link user %></td>
			  <td><%= user.has_challenge? ? challenge_list(user.challenges) : label_name('challenges_none') %></td>		 
			  <td><%= user.time_zone %></td>			  
			  <td><%#= nice_day_date(cast.starts_at) unless cast.nil? %></td>	
			  <td class="last"><%= link_to label_name(:send_message_to), new_message_path(:id => user) unless current_user == user %></td>		  
			</tr>
        <% end -%>
		
      </table>
	  <%= render :partial => 'shared/action_bar', :locals => { :collection => @users } %>
	  
    </div>
	
  </div>
  
</div>

<% content_for :sidebar, render(:partial => 'sidebar_show') -%>