<%#= render 'sidebars/sidebar_edit_zurb', :item => @schedule %>

<% 
	the_label ||= label_name(:options)
	
	is_displayable = true
	
	case item.class.to_s
	when 'Type', 'Role', 'Announcement', 'Sport'
		is_displayable = the_maximo
	when 'User'
		is_displayable = (is_current_same_as(item))
	when 'Group', 'Cup', 'Challenge'
		is_displayable = is_current_manager_of(item)
	when 'Schedule', 'Classified', 'Fee', 'Payment'
		is_displayable = is_current_manager_of(item.group)
	end	
%>

<%= set_form_edit_image_link(item) if is_displayable %>
