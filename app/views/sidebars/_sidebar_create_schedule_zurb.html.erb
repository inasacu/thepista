<%
	@current_manage_groups = current_user.get_current_manage_groups
%>

<% @current_manage_groups.each do |group| %>
	
	<%= set_form_create_id_label_link(Schedule.new, :id, group, "#{label_name(:schedules_create)} - #{group.name}") unless group.is_branch? %>
	
<% end %>


