
  <div class="group">
    <%= f.label :concept, label_name('name'), :class => :label %>
    <%= f.text_field :name, :class => 'textfield' %>
    <span class="description"><%= control_label('name_description') %></span>
  </div>

  <div class="group">
    <%= f.label :table_type, label_name('table_type'), :class => :label %>
    <%= f.text_field :table_type, :class => 'textfield' %>
    <span class="description"><%= label_name('table_type', 'description') %></span>
  </div>

  <div class="group">
    <%= f.label :table_id, label_name('table_id'), :class => :label %>
    <%= f.text_field :table_id, :class => 'textfield' %>
    <span class="description"><%= label_name('table_id', 'description') %></span>
  </div>

<div class="clear"></div> 

<div class="group navform">
	<%= submit_tag @type.new_record? ? control_label('create') : control_label('edit'), 
			:class => "button" %> <%= label_name(:or) %> <%= link_to label_name(:cancel), types_path %>
</div>