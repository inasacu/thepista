<% 		
	get_first_active ||=""
	get_first_active = get_active("#{get_the_controller}_#{get_the_action}") if is_controller('prospect') and is_action('index') 
	
	the_label = "" 
	the_action = ['show','edit']
	
	is_single_prospect = false
	if @prospect 
		is_single_prospect = true
	end
	content_for(:description, "#{@prospect.name}") if @prospect
%>
	<%= set_tab_navigation(link_to(label_name(:venues), prospects_path, set_tab_navigation_active('prospect_index'))) unless @prospect %>
	<%#= set_tab_navigation(item_name_link(@prospect, set_tab_navigation_active('prospect_show'))) if is_single_prospect %>
