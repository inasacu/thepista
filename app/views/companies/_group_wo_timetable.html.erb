<%
	the_label = " #{label_name(:only_put)} #{I18n.t(:create).downcase} #{label_name(:timetable).downcase} #{label_name(:for)} "
	
	the_first_team_link, has_group_timetable, the_team_link = get_company_team_link(the_company)
%>

<% unless the_first_team_link %>
	<hr/>
	<%= content_tag :h6, "#{the_label} #{the_team_link}".html_safe, :class => "subheader" %>
	<hr/>
<% end %>