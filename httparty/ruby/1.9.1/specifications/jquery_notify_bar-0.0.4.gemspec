# -*- encoding: utf-8 -*-

Gem::Specification.new do |s|
  s.name = "jquery_notify_bar"
  s.version = "0.0.4"

  s.required_rubygems_version = Gem::Requirement.new(">= 0") if s.respond_to? :required_rubygems_version=
  s.authors = ["Jose Carlos Ustra Junior"]
  s.date = "2011-07-26"
  s.description = "A simple gem to include jquery.notifyBar to your rails app."
  s.email = ["contato@ustrajunior.com"]
  s.homepage = "http://ustrajunior.com/apps/jquery_notify_bar"
  s.require_paths = ["lib"]
  s.rubyforge_project = "jquery_notify_bar"
  s.rubygems_version = "1.8.16"
  s.summary = "jquery.notifyBar in your app"

  if s.respond_to? :specification_version then
    s.specification_version = 3

    if Gem::Version.new(Gem::VERSION) >= Gem::Version.new('1.2.0') then
    else
    end
  else
  end
end
