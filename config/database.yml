# SQLite version 3.x
#   gem install sqlite3-ruby (not necessary on OS X Leopard)
development:
  adapter: mysql
  database: desarrollo_thepista
  username: root
  password: 72dae4
  host: localhost
  encoding: utf8    # for MySQL
  reconnect: true

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
      adapter: mysql
      database: prueba_thepista
      username: root
      password: 72dae4
      host: localhost
      encoding: utf8    # for MySQL
      reconnect: true

production:
      adapter: mysql
      database: produccion_thepista
      username: root
      password: 72dae4
      host: localhost
      encoding: utf8    # for MySQL
